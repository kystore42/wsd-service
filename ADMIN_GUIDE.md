# 🎛️ Admin Panel Guide

## Как использовать админ-панель

### Активация админ-панели

**Способ 1: Горячая клавиша**
- Нажмите `Ctrl + /` на нужной странице
- Введите пароль: `eratosfen`

**Способ 2: URL параметр**
- Откройте страницу с параметром: `https://example.com/?admin=true`
- Введите пароль при появлении окна

## Какие панели есть

### 1) Главная страница (index.html)
Админ-панель управляет контентом главной страницы.

Вкладки:
- **Stats** - числа в блоке статистики
- **Testimonials** - отзывы на главной (если включены)

Кнопки:
- **Save Changes** - сохранить в `localStorage`
- **Reset to Default** - сбросить к данным из `content.json`

### 2) Отзывы и галерея (reviews-gallery.html)
Отдельная админ-панель для отзывов и галереи.

Вкладки:
- **Отзывы** - текст, фото, видео отзывов
- **Галерея** - фото/видео элементов галереи

Кнопки:
- **Сохранить изменения** - сохраняет в `localStorage`
- **Сбросить к исходным** - загрузка из `admin-data.json`
- **Скачать admin-data.json** - экспорт текущих данных

## Где хранятся данные

1. **Главная страница**
- Данные: `content.json`
- Хранилище: `localStorage` браузера

2. **Отзывы и галерея**
- Данные: `admin-data.json`
- Хранилище: `localStorage` браузера (ключ `adminDataV2`)

⚠️ **Ограничение**: `localStorage` имеет лимит по размеру. Большие фото/видео могут не сохраниться.

## Безопасность

- Текущий пароль: `eratosfen`
- Изменяется в [admin.js](admin.js#L2) и [reviews-gallery.js](reviews-gallery.js#L2)
- Для реальной защиты нужен backend (валидация на сервере)

## Полезные советы

- Для видео используйте ссылки (YouTube/Vimeo/файл на сервере)
- Для фото лучше оптимизировать размер перед загрузкой

## 📞 Поддержка

Если что-то не работает, проверьте консоль браузера и размер загружаемых файлов.
